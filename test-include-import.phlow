name: test-include-import
version: 1.0.0
description: Test file for !import and !include functionality

# Test !include directive
config: !include test-config.yaml

# Test !import directive
helper: !import test-helper.phs

steps:
  - payload: !phs helper.get_greeting(main.name)
  - payload: !phs `Config loaded: ${config.database.host}`
  - return: !phs payload

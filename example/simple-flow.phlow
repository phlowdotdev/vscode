name: Phlow Simples
version: 1.0.0
description: Exemplo de phlow simples sem módulos externos
author: Phlow Team

steps:
  - payload:
      message: "Olá, mundo!"
      timestamp: !phs `now()`
  - payload: !phs `${payload.message} - Processado em ${payload.timestamp}`
  - return: !phs payload

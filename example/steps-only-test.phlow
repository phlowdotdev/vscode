# Arquivo apenas com steps (sem main raiz)

tests:
  - describe: "Test without root main"
    main:  # Este main não deve ter CodeLens
      cli:
        command: "echo 'test'"
    assert_eq: "test"

steps:  # Este deve ter o CodeLens "Run Phlow"
  - payload: "step 1"
  - main:  # Este main aninhado não deve ter CodeLens
      cli:
        command: "nested"
